<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.myMapper">
	<!-- borad -->
	<select id="getBoardList" resultType="model.Board">
		select bid, cate_cd, title, content, tag, view_cnt, reg_id, reg_dt, edit_dt
		from board
		where bid =#{bid}
	</select>
	
	<select id="getBoardContent" resultType="model.Board" parameterType="model.Board">
		select bid, cate_cd, title, content, tag, view_cnt, reg_id, reg_dt, edit_dt
		from board
		where bid=#{bid}
	</select>
	<insert id="insertBoard" parameterType="model.Board">
		insert into board (cate_cd, title, content, tag, view_cnt, reg_id, reg_dt, edit_dt)
		value(#{cate_cd),#{title},#{content},#{tag}, null, #{reg_id}, select to_char(sysdate,'yyyymmdd hh24:mi:ss') from dual, select to_char(sysdate,'yyyymmdd hh24:mi:ss') from dual)
	</insert>
</mapper>






